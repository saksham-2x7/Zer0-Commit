# DRISHYA'S FOLDER

Scaffold your React + Vite app here. Read `../backend/api/CONTRACT.md` for the
exact request/response shape of `POST /api/analyze` before writing fetch code.

Don't touch anything outside this folder except `infra/template.yaml`, and only
to append your own resources below the marked line.
